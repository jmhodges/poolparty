grammar PPallowedtypes
  include PPnumbers
  rule range
    s integer ".." integer
  end
  rule array
    s '[' s allowed_types s (comma s allowed_types) s ']'
  end
  rule allowed_types
    range / numeric / string
  end
  rule s  
    [ \t\r\n]*
  end
end